# KakaoSapiens 개선 백로그

작성일: 2026-08-14 · 최종 갱신: 2026-08-14
상태: **1·2·5·6·7·12 적용 완료 · 13 부분 완료 · 나머지 미착수**

이 문서는 Gemini 3.7 Flash 이식 작업 중에 코드를 훑어보며 발견한 개선 후보를 모아둔 것이다.
각 항목은 이 대화 기록 없이도 바로 착수할 수 있도록 **현상 → 근거 → 제안** 순으로 적었다.
직접 실측한 수치는 [부록](#부록-실측으로-확인한-사실들)에 따로 정리했다.

---

## 이미 적용된 것 (재작업 방지용)

아래는 **이미 끝난** 작업이다. 백로그가 아니다.

| 항목 | 내용 |
|---|---|
| 모델 이식 | `gemini-3.6-flash` → `gemini-3.7-flash`, 레거시 식별자 마이그레이션 포함 |
| 요금 정정 | 도입 요금(입력 $0.75 / 출력 $3.75 / 캐시 $0.075) 반영, 2027-01-01 자동 전환 |
| API 키 | 소스 하드코딩 → macOS 키체인, `x-goog-api-key` 헤더 전송 |
| git 히스토리 | 노출된 키를 전체 히스토리에서 제거 후 force push |
| 컨텍스트 캐싱 | implicit → **명시적 캐싱(cachedContents)**, 실측 적중률 99.4% |
| 마크다운 `---` | 수평선이 렌더러 판별식에 안 걸리던 문제 수정 |
| 마크다운 볼드 | CommonMark flanking 규칙 때문에 한국어 조사 앞에서 깨지던 문제 수정 |
| 채팅 스크롤 | 웹뷰 높이가 늦게 확정되어 덜 내려가던 문제 수정 |
| 수식 가로 잘림 | 말풍선 폭 초과 시 글자 크기 자동 축소 |
| **1. 로컬 번들** | KaTeX·markdown-it을 앱 번들로. 오프라인 렌더링 확인(`fontLoaded: true`) |
| **2. 웹뷰 부담** | 말풍선마다 문서를 새로 로드하던 것 → 고정 셸 1회 로드 + 내용만 주입 |
| **5. 사용량 추정치** | 글자 수 기반 추정 부트스트랩 제거. 실제 API 보고값만 기록 |
| **6. 캐시 보관료** | `cacheStorageTokenHours` 추가, 원가에 반영. 캐시 쓰기 배수도 모델별 분리 |
| **7. 그래프 파서** | 재귀 하강 파서로 교체. 해석 실패 시 그래프를 만들지 않음 |
| **12. ATS 전면 해제** | `NSAllowsArbitraryLoads` 제거 (외부 네트워크 요청 자체가 사라짐) |
| 이미지 깜빡임 | 첨부 이미지 디코드 캐시 추가 |
| 스크롤 버벅임 | 저장을 0.7초 단위로 모으고, 방 목록 갱신을 실제 변경 시에만 |
| 말투 페르소나 | 방별 말투 설정 + Gemini 말투 분석 (신규 기능) |

### ⚠️ 13. 코드 서명 — 부분 완료

`build_app.sh`에 서명 단계를 넣었지만, **이 맥에 코드 서명 인증서가 없어 여전히 ad-hoc으로 서명됩니다.**
따라서 재설치할 때마다 키체인 접근 창이 계속 뜹니다. 없애려면 사용자가 인증서를 만들어야 합니다.

```
키체인 접근 > 인증서 지원 > 인증서 생성
  이름: 아무거나 (예: KakaoSapiens Dev)
  유형: 코드 서명
  자체 서명 루트
```

만든 뒤에는 `build_app.sh`가 자동으로 찾아 씁니다. 이름을 직접 지정하려면:

```bash
CODESIGN_IDENTITY="KakaoSapiens Dev" ./build_app.sh
```

---

## 🔴 우선순위 높음

### ✅ 1. KaTeX·markdown-it·Pretendard를 로컬 번들로 옮기기 — 적용 완료

**현상**
수식이 들어간 말풍선마다 `cdn.jsdelivr.net`에 네트워크 요청을 보낸다.

**근거**
`Sources/KakaoSapiens/Views/LaTeXMarkdownView.swift` 의 `KaTeXHTMLGenerator.generateHTML` 안 `<head>`
— Pretendard CSS, KaTeX CSS/JS, auto-render, markdown-it 총 5개를 CDN에서 받는다.

**왜 문제인가**
- 인터넷이 끊기면 **수식이 아예 렌더링되지 않는다.**
- 첫 렌더링이 네트워크 왕복만큼 늦다. 채팅 스크롤이 덜 내려가던 버그의 **근본 원인**이 이 지연이었다
  (지금은 높이 확정 알림으로 보정했지만, 로컬 번들로 바꾸면 보정 자체가 거의 불필요해진다).
- 외부 CDN에 대한 가용성 의존이 생긴다.

**제안**
`Package.swift`에 이미 `resources: [.process("Resources")]`가 선언되어 있으므로 넣을 자리는 준비되어 있다.
KaTeX 배포본(폰트 포함)과 markdown-it, Pretendard를 `Sources/KakaoSapiens/Resources/`에 넣고
`loadHTMLString(baseURL:)`을 번들 URL로 지정하거나 `loadFileURL(_:allowingReadAccessTo:)`로 바꾼다.

> KaTeX는 폰트 파일(`fonts/`)까지 같이 넣어야 수식이 제대로 나온다. 이걸 빠뜨리면 글자가 깨져 보인다.

**부수 효과**: 12번(ATS 전면 해제)을 자연스럽게 같이 해결할 수 있다.

**난이도**: 중 · **효과**: 큼

---

### ✅ 2. 말풍선 하나당 WKWebView 하나인 구조 — 적용 완료

**현상**
`containsLaTeXOrMarkdown`이 참인 메시지마다 독립된 `WKWebView`가 생성된다.

**근거**
`Sources/KakaoSapiens/Views/MessageBubbleView.swift:88`, `:219` — 조건부로 `LaTeXMarkdownView`를 띄운다.
`LazyVStack` 안이라 화면 밖은 해제되지만, 긴 대화를 스크롤하면 수십 개가 반복 생성·해제된다.

**왜 문제인가**
웹뷰 하나가 각각 JS 엔진과 렌더링 컨텍스트를 갖는다. 메모리·CPU 부담이 크고,
스크롤할 때마다 재생성되며 CDN 로드(1번)가 다시 일어난다.

**제안**
- 단기: 1번(로컬 번들)만 해도 재생성 비용이 크게 줄어든다.
- 중기: KaTeX 렌더 결과를 `NSAttributedString`으로 한 번 변환해 메시지 ID 기준으로 캐시하고,
  웹뷰는 최초 변환 때만 쓴다. 수식 없는 부분은 지금처럼 네이티브 `Text`로 간다.

**난이도**: 높음 · **효과**: 큼

---

### 3. 스트리밍 응답 도입

**현상**
답변 전체를 받은 뒤에야 화면에 뿌린다. 그동안 타이핑 인디케이터만 보인다.

**근거**
`Sources/KakaoSapiens/Services/GeminiService.swift` — `:generateContent` 단발 호출.
`Sources/KakaoSapiens/Views/SingleChatRoomView.swift:389` 부근 — 응답을 다 받은 뒤 말풍선을 순차 표시.

**왜 문제인가**
긴 수학 풀이는 10~30초씩 걸린다. 게다가 말풍선마다 **450ms 강제 지연**이 붙어 있어
(`SingleChatRoomView.swift:391`, `Task.sleep(nanoseconds: 450_000_000)`),
10개로 쪼개지면 **이미 다 받은 답변을 4.5초 더 기다린다.**

**제안**
`:streamGenerateContent`(SSE)로 바꾸고 도착하는 대로 말풍선을 채운다.
스트리밍이 들어가면 450ms 지연은 의미가 없어지므로 같이 걷어내거나 크게 줄인다.

> 주의: 명시적 캐싱과 사용량 집계는 스트림 **마지막 청크**의 `usageMetadata`에서 읽어야 한다.
> 지금 캐시 갱신 로직(`refreshPrefixCache`)은 응답 확정 후 호출되므로 그 시점을 그대로 유지하면 된다.

**난이도**: 중 · **효과**: 체감 속도 개선 가장 큼

---

### 4. 모델 선택을 방별로 기억하기

**현상**
방 이름은 `GPT 5.6 Luna`인데 헤더의 모델 표시는 `Gemini`인 상태가 나온다.

**근거**
`Sources/KakaoSapiens/Views/SingleChatRoomView.swift:372` — `modelManager.selectedModel`을 그대로 쓴다.
`ModelSelectionManager`는 싱글턴이고 선택값을 `UserDefaults`에 **전역 하나**로 저장한다
(`Sources/KakaoSapiens/Models/AIModel.swift`).
`ChatRoom`에는 모델 필드가 없다.

**왜 문제인가**
방을 바꿔도 모델이 따라오지 않아 이름과 실제가 어긋난다.
사용자가 방 이름으로 모델을 구분해 쓰고 있으므로 혼동이 크다.

**제안**
`ChatRoom`에 `preferredModel: AIModel?`을 추가하고, 없으면 전역값으로 폴백한다.
요금 집계는 이미 방별·모델별로 나뉘어 있어(`TokenUsageManager.usageByRoom`) 자연스럽게 맞아떨어진다.

**난이도**: 낮음 · **효과**: 중

---

## 🟡 정확성 문제

### ✅ 5. 사용량 대시보드에 추정치가 섞여 있다 — 적용 완료

**현상**
실제 API가 보고한 토큰과, 과거 대화를 글자 수로 추정한 값이 한 숫자로 합쳐진다.

**근거**
`Sources/KakaoSapiens/Services/TokenUsageManager.swift:239`
```swift
let estimate = max(Int(Double(message.text.count) * 1.6), 10)
if message.sender == .user { input += estimate + 250 } else { output += estimate }
```

**왜 문제인가**
실측한 한국어 토큰 밀도는 **약 2.4자당 1토큰**이다(부록 참조). 즉 올바른 계수는 `× 0.42` 근처인데
지금은 `× 1.6`이라 **약 4배 과대 집계**다. 게다가 이 추정치가 실제 청구액과 구분 없이 합산되어
대시보드 숫자 전체의 신뢰도를 떨어뜨린다.

**제안**
셋 중 하나.
1. 부트스트랩 자체를 없앤다 (가장 깔끔)
2. 계수를 `0.42`로 고치고 추정분을 별도 필드로 분리해 "추정치 포함"으로 표시
3. 추정분을 원가 계산에서 제외하고 토큰 수만 참고로 보여준다

**난이도**: 낮음 · **효과**: 중 (신뢰도)

---

### ✅ 6. 캐시 보관 요금이 원가 계산에 없다 — 적용 완료

**현상**
명시적 캐싱 도입으로 `$0.50 / 1M 토큰 / 시간`의 보관 요금이 생겼는데 어디에도 반영되지 않는다.

**근거**
`Sources/KakaoSapiens/Services/TokenUsageManager.swift` — `costUSD(for:)`는 입력·캐시읽기·쓰기·출력만 계산한다.
캐시 TTL은 `GeminiService.cacheTTLSeconds = 900`(15분).

**규모**
12,000토큰을 15분 보관하면 턴당 약 **$0.0015**. 같은 턴의 절감액이 약 $0.008이므로 순이익은 확실하지만,
표시되지 않아 실제 청구서와 미세하게 어긋난다.

**추가**
원가 공식의 `writes × 1.25`는 OpenAI의 캐시 쓰기 개념이다. Gemini에는 해당 항목이 없어
`cacheWriteTokens`가 항상 0이라 지금은 무해하지만, 모델별로 공식을 분리하는 편이 옳다.

**난이도**: 낮음 · **효과**: 낮음~중

---

### ✅ 7. 그래프 지원 식이 좁고, 벗어나면 조용히 실패한다 — 적용 완료

**현상**
지원 식이 `sin(x), cos(x), tan(x), x^2, exp(x), ln(x), t*cos(t), t*sin(t)`뿐이고,
시스템 지침으로 모델에게 "이것만 쓰라"고 요청해 막고 있다.

**근거**
`Sources/KakaoSapiens/Services/GeminiService.swift` — `stableSystemPrompt`의 `# 그래프` 절.
`Sources/KakaoSapiens/Services/MathGraphRenderer.swift`.

**왜 문제인가**
모델이 지침을 어기고 `x^3`이나 `1/x` 같은 식을 내보내면 파서가 처리하지 못해
**빈 그래프 또는 잘못된 그래프가 조용히 나간다.** 수학 학습 앱에서 틀린 그래프는 위험하다.

**제안**
- 파서를 다항식·유리식 정도까지 넓힌다.
- 그리고 **미지원 식은 그래프를 만들지 않고 건너뛴다.** 지금처럼 조용히 이상한 그림을 내보내지 않는다.

**난이도**: 중 · **효과**: 중 (정확성)

---

## 🟢 다듬을 것

### 8. 실패한 요청 재시도 버튼
오류가 `"요청을 처리하는 중 오류가 발생했습니다: …"` 말풍선으로 남고 끝이다
(`SingleChatRoomView.swift:413`). 대화 이력에서는 걸러지지만 화면에는 영구히 남는다.
해당 말풍선에 재시도 버튼을 달면 좋겠다.

### 9. 응답 취소
`isTyping` 가드로 중복 전송은 막지만, 진행 중인 응답을 취소할 방법이 없다.
잘못 보낸 질문도 토큰을 끝까지 다 쓴다. `Task` 핸들을 보관하고 취소 버튼을 노출한다.

### 10. 검색 버튼이 동작하지 않는다
`SingleChatRoomView.swift:52` 에서 `onSearchTapped: nil`로 넘어간다. 눌러도 아무 일이 없다.
구현하거나 버튼을 숨긴다. (전화·영상 버튼도 같은 성격인지 확인 필요)

### 11. 다크 모드 미지원 + 고정 폭
`Info.plist`의 `NSRequiresAquaSystemAppearance`는 `false`인데 색상이 전부 하드코딩이라
다크 모드에서 흰 말풍선에 검은 글씨가 그대로 나온다.
말풍선 폭도 `maxWidth: 320` 고정이라(`MessageBubbleView.swift:221`) 창을 키워도 넓어지지 않는다.
**수식은 폭이 곧 가독성**이므로, 창 크기에 반응하게 하면 자동 축소가 덜 일어나 더 잘 읽힌다.

### ✅ 12. ATS 전면 해제 — 적용 완료
`build_app.sh`가 생성하는 `Info.plist`에 `NSAllowsArbitraryLoads: true`가 들어 있다.
접속처가 전부 HTTPS라 필요 없다. 1번(로컬 번들)을 하면 CDN 의존이 사라져 함께 제거 가능.

### ⚠️ 13. 키체인 접근 창이 재설치마다 뜬다 — 부분 완료 (인증서 생성 필요)
앱이 ad-hoc 서명(Team ID 없음)이라 빌드할 때마다 코드 서명이 바뀌고,
그때마다 macOS가 키체인 접근 허용을 다시 묻는다.
무료 Apple 개발자 계정으로 서명 인증서를 만들어 `build_app.sh`에 서명 단계를 넣으면 사라진다.
(같은 작업으로 Touch ID 기반 키체인 보호도 열린다 — 부록 참조)

---

## 부록: 실측으로 확인한 사실들

문서화 시점에 직접 API·WKWebView를 돌려 확인한 값이다. 추측이 아니다.

### Gemini 컨텍스트 캐싱

| 항목 | 값 |
|---|---|
| implicit 최소 토큰 | 4,096 (문서 기준) |
| **명시적 캐시 최소 토큰** | **1,024** (실측: 1,021 실패 / 1,701 성공) |
| implicit 실측 적중률 | 채팅처럼 턴이 늘어나는 패턴에서 **0%** (7,300토큰, 12초 간격, 5회 연속) |
| implicit이 걸리는 조건 | 요청이 **완전히 동일**할 때만 (접두사 일치만으로는 안 걸림) |
| 명시적 캐시 실측 적중률 | **99.2 ~ 99.5%** |

> 이 때문에 implicit에 기대는 설계는 이 앱에서 통하지 않는다. 명시적 캐싱이 유일한 답이었다.

### 한국어 토큰 밀도
약 **2.4자당 1토큰**. (한국어 수학 대화 24턴 ≈ 2,400자 → 1,021토큰)

### markdown-it / CommonMark
닫는 `**` 바로 앞이 **구두점**이고 바로 뒤가 **글자**면 강조가 닫히지 않는다(right-flanking 규칙).
한국어는 `**공식(확장)**을`처럼 괄호·마침표 뒤에 조사가 붙어 자주 걸린다.
→ 강조 안쪽 끝에 폭 없는 공백(U+200B)을 넣어 우회 중.

### KaTeX CSS 우선순위
`.katex { font-size: … !important }` 가 인라인 스타일을 이긴다.
JS로 크기를 조정하려면 `setProperty(…, 'important')`를 써야 한다.

### 수식 자동 축소 실측 (WKWebView, 말풍선 320px)
| 수식 | 축소 전 | 축소 후 |
|---|---|---|
| `f(x)=2e^x+2e^{2x} ⟹ …` | 344px (잘림) | 316px (글자 16.5→15.0px) |
| `x₂ = x₁ - f(x₁)/f'(x₁) = …` | 382px (잘림) | 316px (글자 16.5→13.5px) |
| `5/3` | 316px | 316px (변화 없음) |

### ad-hoc 서명의 한계
| 시도 | 결과 |
|---|---|
| `kSecAttrAccessControl`(Touch ID) 저장 | `-34018` 엔타이틀먼트 없음 |
| ad-hoc 서명 + 엔타이틀먼트 주입 | 실행 즉시 **SIGKILL** |
| 접근제어 없는 일반 키체인 저장 | 정상 |

→ Touch ID로 키체인을 잠그려면 정식 서명 인증서가 필요하다. 13번 항목과 연결된다.

### Gemini 3.7 Flash 요금 (2026-12-31까지 도입 요금)
| | 도입 요금 | 2027-01-01부터 |
|---|---|---|
| 입력 / 1M | $0.75 | $1.50 |
| 출력 / 1M | $3.75 | $7.50 |
| 캐시 읽기 / 1M | $0.075 | $0.15 |
| 캐시 보관 / 1M / 시간 | $0.50 | $1.00 |

---

## 남은 것과 착수 순서

1. **3번 (스트리밍)** — 이제 가장 체감이 큰 항목. 450ms 강제 지연도 함께 정리
2. **4번 (방별 모델)** — 난이도 낮고 혼동이 바로 사라짐. 말투가 방별이 된 지금 더 자연스러움
3. **11번 (다크 모드·반응형 폭)** — 수식은 폭이 곧 가독성
4. **8·9·10번** — 재시도·취소·검색
5. **13번** — 인증서만 만들면 끝 (위 안내 참조)
