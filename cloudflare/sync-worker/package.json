{
  "name": "gagaodok-sync-worker",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Local-only Phase 1 sync Worker. Never deployed from this package.",
  "scripts": {
    "test": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.8.19",
    "@cloudflare/workers-types": "^5.20260827.1",
    "typescript": "^5.6.3",
    "vitest": "~3.0.9",
    "wrangler": "^4.0.0"
  }
}
