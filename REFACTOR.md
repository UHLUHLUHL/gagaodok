# 무엇을, 왜, 어떻게 나눴는가

이 문서는 2026-08-17에 한 파일 분리 작업의 근거와 기대 효용을 정리한 것입니다.
"어디에 무엇이 있는지"는 [ARCHITECTURE.md](ARCHITECTURE.md)를 보십시오. 이 문서는
그 반대쪽 질문 — **왜 나눴고, 나눠서 뭐가 좋아지는지** — 를 다룹니다.

## 계기

"유지보수할 때 읽어들여야 하는 코드가 길어서 토큰 사용량이 많은 것 같다"는 관찰에서
시작했습니다. 짐작이 아니라 먼저 쟀습니다.

## 잰 것

| | 줄 수 | 저장소 대비 |
|---|---|---|
| `GeminiService.swift` (맥) | 1,738줄 | 맥 전체(10,329줄)의 **17%** |
| `AIService.kt` (안드로이드) | 1,482줄 | 안드로이드 전체(9,980줄)의 **15%** |
| `ChatRoomScreen.kt` (안드로이드) | 1,080줄 | 안드로이드 전체의 11% |

세 파일 합쳐서 **안드로이드 코드의 26%가 파일 두 개에** 있었습니다. 이 셋이
2등부터의 크기를 압도적으로 앞섰습니다 — 4위인 `SingleChatRoomView.swift`가 777줄로,
1위(`GeminiService.swift`)의 절반이 안 됩니다. 즉 "전체가 조금씩 크다"가 아니라
**소수의 파일이 유독 크다**는 것이 실측으로 나왔습니다.

이게 왜 토큰으로 이어지는가: 이 저장소에서 캐시·토큰 로직을 한 줄 고치려면
그 코드가 들어 있는 `GeminiService.swift`를 읽어야 하는데, 그 파일에는 캐시와
아무 상관 없는 말투 조사, OpenAI 연동, 말풍선 분리 로직까지 같이 들어 있습니다.
**한 줄을 고치기 위해 상관없는 코드까지 읽는** 구조였습니다. 실제로 지난 작업
(요금 항목 15개 수정)에서 `GeminiService.swift`를 여러 차례 전체 또는 큰 구간으로
다시 읽었는데, 그때마다 매번 캐시·장부·말투·OpenAI 코드가 한 덩어리로 딸려 왔습니다.

## 무엇을 나눴나

파일 하나를 "하는 일"별로 여러 파일로 갈랐습니다. **코드 내용은 한 글자도
안 바꿨습니다** — 자리만 옮겼습니다. 새 로직을 추가하거나 동작을 바꾸는 작업이
아니라 순수한 재배치입니다.

### 맥: `GeminiService.swift` (1,738줄) → 8개 파일

| 파일 | 줄 | 담긴 것 |
|---|---|---|
| `GeminiService.swift` | 111 | 액터 자신, 상태, 공개 진입점 두 개 |
| `+Conversation.swift` | 327 | 대화 한 턴 요청 조립·전송 |
| `+PrefixCache.swift` | 275 | 명시적 캐시 만들기·재사용·폐기 규칙 |
| `+Digest.swift` | 77 | 구간 요약 |
| `+Persona.swift` | 446 | 말투 찾기·미리보기·다듬기 |
| `+OpenAI.swift` | 214 | Luna(OpenAI) 쪽 길 |
| `+Transport.swift` | 159 | 요청 한 건 보내기, 오류 해석, 장부 기록 |
| `+Bubbles.swift` | 195 | 답변을 말풍선으로 가르기 |

### 안드로이드: `AIService.kt` (1,482줄) → 8개 파일, `ChatRoomScreen.kt` (1,080줄) → 4개 파일

같은 이름·같은 경계로 나눴습니다. `+PrefixCache` ↔ `AIServicePrefixCache`,
`+Persona` ↔ `AIServicePersona` 식입니다. 화면 쪽은 `ChatRoomScreen`(화면 뼈대) /
`ChatRoomHeader`(193줄) / `ChatRoomInputBar`(452줄) / `ChatRoomTyping`(135줄)으로
갈랐습니다.

## 어떻게 나눴나 (그리고 어떻게 검증했나)

손으로 옮기지 않고 **경계를 기계적으로 찾아 스크립트로 옮겼습니다.** 이유는
간단합니다 — 1,700줄짜리 파일을 손으로 자르다 보면 줄을 빠뜨리거나 중복시키기
쉽고, 그 실수는 컴파일 오류로도 안 잡힐 수 있습니다(예: 조건문 안쪽 절반만 옮기면
문법은 맞지만 동작이 바뀝니다).

1. 최상위 선언(함수·프로퍼티·중첩 타입)의 시작 줄을 정규식으로 전부 찾습니다.
2. "하는 일"별로 이름을 묶어 어느 파일로 갈지 정합니다.
3. 스크립트가 원본에서 잘라 새 파일에 붙입니다.
4. **원본 대 (새 파일 전체를 합친 것)을 줄 단위로 대조**합니다. 들여쓰기와
   `private`/`internal` 표기 차이를 정규화한 뒤 비교했을 때, 남는 차이가
   `extension GeminiService {` 여는 줄 7개(Swift)와 옮긴 함수에 붙은
   `AIService.` 수신자(Kotlin, Kotlin은 부분 클래스가 없어 확장 함수로 옮겨야 함)
   뿐임을 확인했습니다. 그 외 실제 코드 줄은 **한 글자도 다르지 않습니다.**
5. 빌드와 시험으로 재확인했습니다.
   - 맥: 디버그·릴리스 빌드 통과. (맥 판에는 테스트 타깃이 없어 순수 계산부는
     `swiftc`로 직접 묶은 임시 하네스로 별도 검증)
   - 안드로이드: **분리 전후 시험 61개, 실패 0건으로 동일.** 디버그 APK 빌드 통과.

이 검증이 가능했던 것 자체가 근거입니다 — 안드로이드에 있는 61개 시험이 없었다면
"자리만 옮겼다"는 주장을 빌드 통과만으로는 증명할 수 없었을 것입니다(빌드는
로직이 바뀌어도 통과할 수 있습니다). 이 사실 자체가 아래 "안 한 것"에서
다시 나옵니다.

## 기대 효용

**1. 한 작업에서 읽어야 하는 코드의 양이 줄어듭니다.**
"캐시 정책을 고쳐줘" 같은 요청은 이제 `GeminiService+PrefixCache.swift`
275줄(또는 `AIServicePrefixCache.kt` 263줄)만 읽으면 됩니다. 전에는 1,738줄
(또는 1,482줄)을 열어야 그 안에서 캐시 관련 부분을 찾아야 했습니다. 파일이
작아지면 그 파일 전체를 컨텍스트에 올려도 부담이 적고, "필요한 부분만 발췌해
읽는" 부분 읽기의 정확도도 올라갑니다 — 조각 하나가 이미 한 가지 일만 하므로
발췌 경계를 잘못 잡을 여지가 적습니다.

**2. 두 플랫폼 사이의 대응이 명시적입니다.**
이 저장소는 같은 기능을 맥·안드로이드 양쪽에 유지해야 하는 구조입니다
(`MAC_BACKPORT.md`가 그 증거입니다 — 안드로이드에서 먼저 고친 것들이 맥에
누락돼 있었습니다). 파일 이름을 맞춰 놓으면 "안드로이드에서 캐시 버그를
고쳤다 → 맥의 `+PrefixCache.swift`도 봐야 한다"는 연결이 파일 이름만으로
드러납니다. 전에는 이 연결을 사람(또는 에이전트)이 두 개의 1,500줄짜리
파일을 각각 훑어서 재구성해야 했습니다.

**3. 변경의 파급 범위가 파일 경계로 드러납니다.**
사고 리스크 관점에서도 이득입니다. 캐시 로직을 고치다가 실수로 말투 조사
코드를 건드리는 사고는,애초에 그 코드가 다른 파일에 있으면 구조적으로
일어나기 어렵습니다. 커밋 리뷰에서도 "이 커밋이 어떤 파일들을 건드렸는가"만
봐도 변경의 범위가 짐작됩니다.

**4. 안 좋아지는 것도 있습니다 — 그대로 적습니다.**
총 줄 수는 오히려 약간 늘었습니다(`import`문과 파일 헤더 주석이 파일 수만큼
반복되므로). 맥은 1,738 → 1,804줄, 안드로이드 서비스는 1,482 → 1,559줄입니다.
**"전체 코드 총량"은 이 리팩터링의 목표가 아니었습니다.** 목표는 "한 작업에서
열어야 하는 최대 파일 크기"였고, 그건 1,738 → 446(최대 조각)로 줄었습니다.
파일이 늘어난 만큼 탐색(어느 파일을 열지 찾는 것)의 비중이 커지므로,
`ARCHITECTURE.md`의 목차 표가 사실상 이 리팩터링의 절반입니다 — 표 없이
파일만 쪼갰다면 "그 로직이 어느 파일에 있더라"를 찾는 데 드는 비용이
오히려 늘었을 수 있습니다.

## 안 한 것과 이유

**600~780줄로 남은 SwiftUI 화면들**(`SingleChatRoomView.swift` 777,
`PersonaEditorView.swift` 759, `KakaoMainWindowView.swift` 759,
`MessageBubbleView.swift` 607)은 **손대지 않았습니다.** SwiftUI는 `@State`가
뷰 구조체에 묶여 있어서, 서비스 로직처럼 "이 함수 뭉치를 그대로 옆 파일로
옮기면 끝"이 되지 않습니다. 상태를 나누려면 그 상태를 쓰는 하위 뷰까지
다시 설계해야 하는데, 그건 이번처럼 "자리만 옮기고 대조로 증명"하는 방식이
안 통하는 작업입니다 — 로직이 실제로 바뀌므로 시험 없이는 회귀를 못 잡습니다.

여기서 근거가 하나 더 나옵니다: **다음에 이 화면들을 나눌 때는 먼저 시험을
붙여야 안전하다**는 것이 이번 작업으로 확인됐습니다. 서비스 로직을 나눌 때
안드로이드의 61개 시험이 안전망이었던 것과 같은 이유로, 화면을 나눌 때는
지금 있는 0개(맥) 또는 UI 시험 부재로는 같은 방식의 검증을 할 수 없습니다.
`ARCHITECTURE.md`에도 이 상태를 적어 뒀습니다.

## 결론

- 근거: 파일 3개가 안드로이드 코드의 26%를 차지했고, 어느 것을 고쳐도 무관한
  코드까지 같이 읽어야 하는 구조였습니다.
- 조치: 8개(맥 서비스) + 8개(안드로이드 서비스) + 4개(안드로이드 화면) 파일로
  기계적으로 재배치. 코드 내용은 대조로 무손실을 확인.
- 검증: 안드로이드 61개 시험 그대로 통과, 양쪽 빌드(맥 디버그·릴리스, 안드로이드
  디버그 APK) 통과.
- 효용: 최대 단일 파일 1,738 → 446줄. 두 플랫폼 간 대응이 파일명으로 명시적.
  대가로 총 줄 수는 소폭 증가 — 그래서 지도 문서(`ARCHITECTURE.md`)를 같이 만들었습니다.
- 다음: 남은 큰 SwiftUI 화면들은 시험 없이 나누면 위험이 크므로 보류.
