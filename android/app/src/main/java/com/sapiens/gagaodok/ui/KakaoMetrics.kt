package com.sapiens.gagaodok.ui

import androidx.compose.ui.unit.dp

/// 이 앱이 쓰는 치수입니다.
///
/// 처음에는 전부 짐작이었습니다. 지금은 **대부분 실측값입니다.**
/// 사용자가 보낸 실기기 캡처(1440×2936, 밀도 3.75, 화면 폭 384dp)를 화소 단위로 재서
/// 옮겼고, 재는 방법과 원본 화소값은 `MEASURED.md`에 남겼습니다.
///
/// 아직 못 잰 값은 그 자리에 **(짐작)**이라고 적어 두었습니다. 나중에 실측값으로
/// 오해받아 지워지지 않게 하려는 것입니다.
object Metrics {
    /// 목록 화면 좌우 여백. 실측 60화소.
    val screenPadding = 16.dp

    /// 손가락이 닿아야 하는 것의 최소 크기입니다. 접근성 기준이라 실측 대상이 아닙니다.
    val touchTarget = 48.dp

    /// 상단 바 높이. 시스템 상태 표시줄 아래에 놓입니다. 실측 210화소.
    val topBarHeight = 56.dp

    /// 아래 탭바 높이. 제스처 바 위에 놓입니다. 실측 206화소.
    val tabBarHeight = 55.dp

    // MARK: - 목록

    /// 목록 한 줄의 아바타. 실측 180화소.
    val listAvatar = 48.dp
    /// 친구 목록 맨 위 내 프로필. 실측 210화소.
    val myProfileAvatar = 56.dp
    /// 친구 목록의 친구 한 줄. 실측 150화소이고 행 간격은 211화소입니다.
    /// 채팅 목록(48dp)보다 작습니다.
    val friendAvatar = 40.dp
    val friendRowPaddingV = 8.dp
    /// 목록 한 줄의 위아래 여백. 실측 행 간격 285화소에서 아바타를 뺀 값의 절반.
    val listRowPaddingV = 14.dp
    /// 아바타와 글자 사이. 실측 45화소.
    val listAvatarGap = 12.dp
    /// 시각이 놓이는 오른쪽 여백. 실측 63화소.
    val listTrailingPadding = 17.dp
    /// 이름 옆에 붙는 작은 표시(상단 고정·말투). 이름 글자(15sp)보다 작아야 하되
    /// 너무 작으면 무슨 그림인지 안 보입니다. (짐작)
    val rowBadge = 13.dp

    // MARK: - 대화방

    /// 말풍선 옆 아바타. 실측 143화소.
    val bubbleAvatar = 38.dp
    /// 대화방 좌우 여백. 실측: 아바타 왼쪽 끝이 23화소.
    val roomPadding = 6.dp
    /// 아바타 오른쪽 끝과 말풍선 왼쪽 끝 사이. 실측 22화소.
    ///
    /// 6 + 38 + 6 = 50dp이고, 실측한 말풍선 왼쪽 끝 188화소(50.1dp)와 맞습니다.
    val bubbleAvatarGap = 6.dp

    /// 말풍선 최대 폭을 화면 폭의 몇 배로 둘지입니다.
    ///
    /// **(짐작)** 받은 캡처에는 줄바꿈까지 간 말풍선이 없어서 못 쟀습니다.
    /// 긴 답변이 담긴 화면을 한 장 받으면 확정할 수 있습니다.
    const val bubbleMaxWidthFraction = 0.68f

    /// 말풍선 모서리. 실측 49화소.
    val bubbleCorner = 13.dp
    /// 말풍선 안쪽 여백. 가로는 실측 40화소.
    /// 세로는 한 줄짜리 말풍선 높이 130화소에서 되짚은 값입니다.
    val bubblePaddingH = 11.dp
    val bubblePaddingV = 8.dp

    /// 말풍선 사이 간격. 같은 사람이 이어서 말할 때는 좁게 둡니다.
    /// 실측: 같은 묶음 안이 27화소, 묶음 사이가 46화소.
    /// 위아래로 나눠 붙으므로 절반씩 적습니다.
    val bubbleGap = 6.dp
    val bubbleGapSameSender = 3.5.dp
    /// 이름 아래와 첫 말풍선 사이. 실측 27.5화소.
    val bubbleNameGap = 7.dp
    /// 말풍선과 그 옆 시각 사이. 실측 16화소.
    val bubbleTimeGap = 4.dp

    /// 나레이션은 아바타도 이름도 없이 화면 가운데에 놓입니다.
    /// 좌우를 넉넉히 비워야 말풍선과 다른 것으로 읽힙니다. (짐작)
    val narrationInset = 44.dp

    // MARK: - 입력바

    /// 입력바 전체 높이. 실측 181화소.
    val inputBarHeight = 48.dp
    /// 가운데 필드 높이. 실측 135화소.
    val inputFieldHeight = 36.dp

    /// 그 필드의 모서리입니다. **높이의 절반이 아니라 고정값입니다.**
    ///
    /// 처음에 `RoundedCornerShape(percent = 50)`으로 뒀습니다. 한 줄일 때는 맞았지만,
    /// 그 백분율은 상자의 **짧은 변**을 기준으로 계산됩니다. 다섯 줄이 되면 짧은 변이
    /// 높이(약 116dp)라 반지름이 58dp가 되고, 좌우 곡선이 안쪽으로 깊이 파고들어
    /// 첫 줄과 마지막 줄의 글자가 필드 밖으로 삐져나왔습니다.
    ///
    /// 값은 수정 바 필드에서 잰 것을 그대로 옮겼습니다(`editFieldCorner`). 그쪽은
    /// 한 줄(135화소)과 다섯 줄(436화소) **양쪽에서 모서리가 똑같이 67화소**여서
    /// 고정 반지름임이 확인됐고, 입력바 필드도 한 줄 높이가 같은 135화소입니다.
    /// **입력바 자체를 다시 잰 값은 아닙니다.**
    val inputFieldCorner = 18.dp
    /// 좌우의 둥근 단추 지름. 실측 105화소.
    val inputButton = 28.dp
    /// 그 단추 안의 그림 크기입니다.
    ///
    /// **이 값은 잉크가 아니라 상자입니다.** 원조의 글리프 잉크는 49화소인데,
    /// Material 아이콘은 제 상자를 다 채우지 않고 0.8쯤만 씁니다.
    /// 그래서 13dp를 주었더니 잉크가 39화소밖에 안 나왔습니다. 49 ÷ 0.8 ÷ 3.75 ≈ 16dp.
    val inputButtonGlyph = 16.dp

    /// 입력바 오른쪽 여백입니다.
    ///
    /// 왼쪽과 단추 사이는 여백을 따로 주지 않습니다. 48dp 터치 상자가 28dp 원보다
    /// 사방 10dp씩 커서 그것만으로 원조의 여백(왼쪽 36화소, 사이 33화소)이 나옵니다.
    /// 원조는 오른쪽만 59화소로 왼쪽(36)보다 넓어서 그 차이만 여기서 줍니다.
    val inputBarEndPadding = 6.dp

    /// 아이콘 크기. 목록과 대화방이 다릅니다.
    /// 실측: 목록 돋보기 74화소·톱니 78화소, 대화방 돋보기 66화소.
    val headerIcon = 20.dp
    val roomHeaderIcon = 18.dp
    /// 뒤로 화살표. 실측 72화소로 돋보기(66)보다 조금 큽니다.
    ///
    /// Material 화살표는 상자를 다 채우지 않고 잉크가 상자의 0.67쯤이라
    /// 잉크 72화소를 내려면 상자를 그만큼 키워야 합니다. 72 ÷ 0.67 ÷ 3.75 ≈ 28.7dp.
    val backIcon = 29.dp
    /// 아래 탭바 아이콘. 실측 63화소.
    val tabIcon = 17.dp

    // MARK: - 길게 누르기 메뉴
    //
    // 원조는 아래에서 올라오는 시트가 아니라 **화면 한가운데 뜨는 카드**입니다.
    // 실측: 카드 위 467화소·아래 468화소로 정확히 가운데였습니다.

    /// 카드 좌우 여백. 실측 150화소(양쪽 같음). 384dp 화면에서 카드 폭은 304dp가 됩니다.
    val sheetSideMargin = 40.dp
    /// 카드 모서리. 실측 30화소.
    ///
    /// 눈으로는 더 둥글어 보이지만 카드가 1140화소로 넓어서 그렇게 보일 뿐입니다.
    /// 네 점(dy 2·8·14·23)이 모두 반지름 30화소 원에 1화소 안으로 맞았습니다.
    val sheetCorner = 8.dp
    /// 메뉴 한 줄 높이. 실측 잉크 중심 간격이 정확히 180화소였습니다.
    val sheetRowHeight = 48.dp
    /// 줄 글자의 왼쪽 여백. 실측 잉크가 카드 왼쪽에서 92화소.
    /// 글자 왼쪽 여백(사이드 베어링)을 빼면 88화소쯤입니다.
    val sheetRowInset = 24.dp
    /// 카드 위아래 안쪽 여백. 실측: 마지막 줄 아래가 43화소.
    val sheetPaddingV = 11.dp
    /// 뒷배경을 어둡게 하는 정도. 실측: 하늘색 171→85, 흰색 255→127로 둘 다 ×0.497.
    const val sheetScrimAlpha = 0.5f

    // MARK: - 메시지 수정 바
    //
    // 실측 대상은 세 캡처입니다. 키보드가 올라온 것 둘(변경 전·후)과 내린 것 하나.
    // 셋의 배너 치수가 화소까지 같아서 배너는 상태와 무관하게 고정입니다.

    /// 수정 바 좌우 여백. 실측 38화소. 연필 원과 필드 왼쪽이 같은 자리입니다.
    val editSidePadding = 10.dp
    /// 패널 위 끝과 배너 줄 위 끝 사이. 실측 20화소.
    ///
    /// **연필 원 위 끝까지가 34화소이지 이 값이 34화소가 아닙니다.** 배너 줄의
    /// 높이는 원이 아니라 두 줄짜리 글자가 정합니다(원조 126화소 > 원 98화소).
    /// 원은 그 안에서 가운데 정렬이라 14화소 내려가 있습니다. 처음에 원을 기준으로
    /// 34dp를 줬더니 패널이 통째로 67화소 길어졌습니다.
    val editTopPadding = 5.dp
    /// 연필이 든 동그라미. 실측 98화소, 테두리는 #DBDBDB.
    ///
    /// 그 테두리 색은 흰 바탕 위의 `border`(검정 14%)와 정확히 같은 값이라
    /// 색을 새로 만들지 않고 기존 것을 씁니다.
    val editIconCircle = 26.dp
    /// 연필 글리프. 실측 잉크 37화소.
    val editIconGlyph = 12.dp
    /// 동그라미 오른쪽 끝과 "메시지 수정" 사이. 실측 잉크까지 33화소.
    /// 글자 왼쪽 여백(4화소쯤)을 빼면 29화소라 8dp입니다.
    val editIconGap = 8.dp
    /// 배너 닫기 X가 든 상자입니다. 실측 잉크 52화소, 중심이 화면 오른쪽 끝에서
    /// 83.5화소(22dp)입니다. 34dp 상자에 오른쪽 여백 5dp를 주면 중심이 22dp가 됩니다.
    ///
    /// **상자를 44dp로 두면 안 됩니다.** 줄 높이는 가장 큰 아이가 정하는데,
    /// 44dp(165화소)짜리 상자가 두 줄 글자(127.5화소)를 제치고 줄 높이를 잡아
    /// 수정 바 전체가 37화소 길어졌습니다. 원조의 배너 줄은 126화소입니다.
    /// 손가락 과녁이 접근성 권장치(48dp)보다 작지만, 원조가 그만한 자리를 안 씁니다.
    val editCloseBox = 34.dp
    val editCloseEndPadding = 5.dp
    /// 18dp를 줬더니 잉크가 40화소로 원조(52)보다 작았습니다. 52 ÷ 40 × 18 ≈ 23dp.
    val editCloseGlyph = 23.dp
    /// 배너 줄 아래 끝과 필드 위 끝 사이. 실측 42화소.
    /// 여기도 원이 아니라 배너 줄이 기준입니다(`editTopPadding` 설명 참고).
    val editBannerGap = 11.dp

    /// 필드 모서리. 실측 67화소.
    ///
    /// **알약이 아닙니다.** 한 줄일 때는 높이(135화소)의 절반이라 알약처럼 보이지만,
    /// 다섯 줄짜리(436화소) 필드의 모서리를 재도 같은 67화소였습니다.
    /// 알약이면 218화소가 나왔어야 합니다.
    val editFieldCorner = 18.dp
    /// 필드 안쪽 여백. 가로는 실측 잉크 49화소, 세로는 실측 29화소.
    val editFieldPaddingH = 12.dp
    val editFieldPaddingV = 8.dp
    /// 접었을 때 보일 줄 수와 폈을 때 최대 줄 수입니다.
    ///
    /// 접힘은 실측입니다(키보드를 내린 캡처의 필드가 135화소 = 한 줄).
    /// **폈을 때 상한은 짐작입니다.** 받은 캡처의 다섯 줄짜리는 상한에 닿지 않은
    /// 자연스러운 높이였습니다(436화소 = 정확히 다섯 줄).
    const val editFieldMaxLines = 6

    /// 확인 단추. 실측 지름 128화소, 색 #FEE500, 체크는 흰색 잉크 53화소.
    val editConfirm = 34.dp
    val editConfirmGlyph = 20.dp
    /// 필드와 단추 사이. 실측 26화소. 단추 오른쪽 여백도 같은 26화소입니다.
    val editConfirmGap = 7.dp
    /// 단추가 선 상태의 오른쪽 여백. 실측 26화소.
    ///
    /// 원조에는 단추가 없는 상태(오른쪽 여백 38화소)도 있지만 우리는 단추를 늘 두므로
    /// 그쪽 값은 쓰지 않습니다. 이유는 `EditBar` 설명에 적었습니다.
    val editEndPadding = 7.dp
    /// 필드 아래 여백. 실측 24화소(키보드 위·아래 모두 같음).
    val editBottomPadding = 6.dp
}
