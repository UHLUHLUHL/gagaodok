# 가가오독 안드로이드 모바일 판 — 개발 계획

작성 2026-08-16. 계획만 적은 문서이고 아직 코드는 한 줄도 없습니다.

대상은 **휴대폰 세로 화면 하나**입니다. 태블릿은 범위 밖이고, 정해진 해상도가 없으니
전부 반응형으로 짭니다. 가로 화면은 목표가 아니지만 돌렸을 때 깨지지는 않아야 합니다.

기준이 되는 맥 판은 이 저장소의 `Sources/KakaoSapiens`, 9,100줄입니다.
그 앱이 무엇이고 왜 그렇게 만들었는지는 [HANDOFF.md](HANDOFF.md)에 있습니다. 이 문서는
겹치는 설명을 하지 않고 **안드로이드로 옮길 때 달라지는 것**만 다룹니다.

---

## 1. 무엇을 만드는가

맥 판과 같은 앱입니다. 카카오톡처럼 생긴 껍데기 안에서 AI와 수학을 공부합니다.
**닮음 자체가 기능**이라는 점도 그대로입니다. 어설프게 닮으면 안 만든 것만 못합니다.

다만 닮아야 할 대상이 바뀝니다.

| | 맥 판 | 안드로이드 판 |
|---|---|---|
| 흉내 낼 원본 | 카카오톡 **데스크톱** | 카카오톡 **모바일** |
| 화면 구조 | 세로 레일 + 목록 창 + 대화방 창 (창 여러 개) | 아래 탭바 + 전체화면 전환 (화면 하나) |
| 크기 | 창 크기 고정 (목록 500×750, 대화방 405×750) | 화면 크기 제각각, 전부 반응형 |

**맥 레이아웃을 그대로 옮기면 안 됩니다.** 왼쪽 세로 레일은 폭 48pt를 통째로
먹는데 360dp 폰에서 그건 화면의 13%입니다. 카카오톡 모바일이 레일 대신 아래 탭바를
쓰는 이유가 그것입니다. 레일을 옮기는 순간 "안드로이드 최적화"가 아니게 됩니다.

### 기능은 하나도 빼지 않습니다

맥 판에 있는 것 전부입니다. 빠지면 그게 곧 "부족함"입니다.

- 대화방 여러 개, 방마다 프로필·말투·모델이 따로
- 스트리밍 응답, 문단 단위 말풍선 분할
- LaTeX 수식과 마크다운 렌더링
- 수학 그래프 그리기 (매개변수·직교좌표, 접선)
- 사진·파일 첨부, 이미지 크게 보기
- 대화 내 검색, 결과 강조
- 말투(페르소나) 편집기 — 예시 대사 수집, 웹 검색으로 인물 조사, 미리보기
- 토큰 사용량·요금 장부, 캐시 절약분 집계
- 대화 압축 (150턴 넘으면 앞부분 요약으로 대체)
- 라이트·다크·시스템 화면 모드

### 빼는 것

안드로이드에 의미가 없는 것만 뺍니다.

- **창 투명도 슬라이더** — 창이라는 개념이 없습니다
- **여러 창 띄우기** — 화면 전환으로 대신합니다
- **마우스 올림(hover) 효과** — 눌림 효과(ripple)로 바뀝니다

---

## 2. 기술 선택

**Kotlin + Jetpack Compose**로 갑니다.

Compose는 SwiftUI와 사고방식이 거의 같습니다. 상태를 선언하면 화면이 따라오고,
`Modifier` 사슬이 SwiftUI의 수정자 사슬과 1:1로 대응합니다. 9,100줄짜리 SwiftUI를
옮길 때 구조를 다시 짤 필요가 없다는 뜻이라 이식 비용이 가장 낮습니다.
직접 그린 아이콘도 SwiftUI `Path` → Compose `Path`로 거의 그대로 옮겨집니다.

대안을 검토했지만 택하지 않았습니다.

- **Flutter / React Native** — 화면은 빨리 나오지만 "카카오톡과 똑같이"가 목표라
  픽셀 단위로 다투게 되는데, 그때 플랫폼 한 겹이 더 끼어 있으면 손해입니다.
  KaTeX 웹뷰와 파일 접근에서도 다리를 하나 더 놓아야 합니다.
- **Kotlin Multiplatform으로 맥 판과 공유** — 지금 맥 판은 SwiftUI·AppKit에
  깊이 묶여 있어서 공유할 수 있는 건 순수 계산부(압축·토큰 추정·수식 파서)뿐입니다.
  그건 그냥 두 번 쓰는 게 빠릅니다.

### 버전

| 항목 | 값 | 이유 |
|---|---|---|
| minSdk | **26** (Android 8.0) | 적응형 아이콘이 26부터입니다. 실사용 기기 98%대를 덮습니다 |
| targetSdk | **35** (Android 15) | 스토어 요건이자 edge-to-edge 기본값 |
| JDK | **17** | 최신 Android Gradle Plugin 요건. 이 맥에는 11만 있어 새로 깔아야 합니다 |
| 언어 | Kotlin 2.x | |

---

## 3. 반응형 설계 — 이 문서의 핵심

정해진 해상도가 없다는 것이 이 작업의 가장 큰 제약입니다.
맥 판은 창 크기를 앱이 정했지만, 여기서는 **화면이 앱에게 크기를 통보**합니다.

### 대야 할 폭의 범위

| 폭 | 어떤 기기 | 이 폭에서 깨지기 쉬운 곳 |
|---|---|---|
| 320dp | 옛 소형·저가 기기 | 대화방 상단 바 아이콘 4개 + 이름이 넘침 |
| 360dp | **가장 흔함** (갤럭시 S 계열 다수) | 기준으로 삼을 폭 |
| 390–412dp | 최근 대형 기기 | 말풍선이 지나치게 넓어져 읽기 나빠짐 |
| 430dp+ | 초대형·폴더블 펼침 | 위와 같음 |

세로는 640dp(구형)부터 950dp(최신 대형)까지 봅니다.

### 규칙

**1. 고정 dp를 쓸 곳과 비율을 쓸 곳을 나눕니다.**

크기가 커지면 안 되는 것은 dp로 고정합니다.
- 아이콘, 아바타, 탭바 높이, 상단 바 높이, 좌우 여백(16dp), 행 사이 간격

화면을 따라 늘어야 하는 것은 비율이나 `weight`로 둡니다.
- 말풍선 최대 폭, 목록 행의 이름·미리보기 글자 영역, 입력창 폭

**2. 말풍선 최대 폭은 화면 폭의 비율입니다.**

맥 판은 pt로 고정돼 있는데 그대로 옮기면 320dp에서는 넘치고 430dp에서는
왼쪽에 붙은 채 남습니다. 비율로 잡되, **실제 안드로이드 카카오톡을 재서 확정**합니다.
지금은 68% 언저리로 짐작만 하고 있고 이건 아직 근거가 없는 숫자입니다.

**3. 가로 스크롤이 생기면 그건 버그입니다.**

세로 스크롤만 존재해야 합니다. 긴 수식이나 코드 블록만 자기 안에서 가로로 흐르게 하고,
화면 자체는 절대 옆으로 밀리지 않게 합니다.

**4. 글자는 sp, 다만 배율을 묶습니다.**

기기 글꼴 크기 설정을 존중하되 `fontScale`을 **0.85–1.3** 사이로 자릅니다.
안 자르면 접근성 설정을 크게 둔 기기에서 말풍선과 탭바가 무너집니다.
카카오톡도 자기 글꼴 크기 설정을 따로 두고 시스템 배율을 그대로 받지 않습니다.

**5. 화면 가장자리(insets)를 직접 다룹니다.**

targetSdk 35에서는 edge-to-edge가 기본이라 앱이 상태 표시줄과 제스처 바 아래까지
그려집니다. 처리해야 할 곳:
- 상단 바 → `statusBars` 만큼 내리기
- 탭바·입력창 → `navigationBars` 만큼 올리기
- **입력창 → `ime`(키보드)** 를 따라 올라오고, 대화 목록이 그만큼 밀려 올라가기

키보드 대응이 안드로이드 채팅 앱에서 가장 흔한 실패 지점입니다.
`Modifier.imePadding()`과 리스트의 `reverseLayout`을 함께 써서, 키보드가 올라올 때
마지막 말풍선이 가려지지 않게 합니다.

**6. 가로 화면은 목표가 아니지만 깨지진 않게.**

전용 레이아웃은 만들지 않습니다. 세로 레이아웃이 그대로 스크롤되면 충분합니다.
화면 회전으로 상태가 날아가지 않게 `rememberSaveable`과 ViewModel만 챙깁니다.

**7. 폴더블 접힘 화면(280dp 근처)은 범위 밖입니다.**

여기까지 대응하면 얻는 것보다 드는 품이 큽니다. 지금은 320dp를 하한으로 둡니다.

### 맥 판의 실측값은 어디까지 가져오는가

이게 오해하기 쉬운 부분입니다.

| 옮기는 것 | 그대로? | 왜 |
|---|---|---|
| **색** (`KakaoTheme`) | 그대로 | 카카오톡은 맥과 모바일이 같은 색을 씁니다 |
| **아이콘 도형** | 그대로 | 비율이 같습니다. `Path` 좌표를 그대로 옮깁니다 |
| **아이콘·글자 크기, 간격** | **다시 재야 합니다** | 데스크톱 23pt ≠ 모바일. 기준 자체가 다릅니다 |

맥 판에서 아이콘을 ±0.5pt까지 맞춘 그 숫자들은 **데스크톱 카카오톡의 숫자**입니다.
모바일에 그대로 쓰면 안 됩니다. 도형(비율)만 가져오고 크기는 안드로이드 카카오톡
화면을 새로 재서 정합니다. `tools/iconcheck/`의 대조 방식은 그대로 쓸 수 있습니다.

---

## 4. 화면 구성

### 탭 구조

아래 탭바 3개입니다. 실제 카카오톡 모바일과 같습니다.

```
┌────────────────────────┐
│ 상단 바                  │  화면마다 다름
├────────────────────────┤
│                        │
│   내용 (스크롤)           │
│                        │
├────────────────────────┤
│  친구   채팅   설정        │  탭바 (고정 높이)
└────────────────────────┘
```

레일 아이콘 두 개(프로필·말풍선)가 그대로 탭 아이콘이 됩니다.
방금 SVG로 다시 그린 그것들입니다.

### 화면 목록과 맥 판 대응

| 안드로이드 화면 | 맥 판의 무엇 | 비고 |
|---|---|---|
| 친구 탭 | `KakaoMainWindowView`의 친구 목록 | 내 프로필 카드 + 방 목록 |
| 채팅 탭 | 같은 파일의 채팅 목록 | 최근 메시지·시각·안 읽은 수 |
| 설정 탭 | `KakaoUsageSettingsView` | 요금·모델·화면 모드·API 키 |
| 대화방 | `SingleChatRoomView` (별도 창) | **전체화면으로 전환** |
| 방 프로필 | `RoomProfileModal` | 아래에서 올라오는 시트 |
| 말투 편집 | `PersonaEditorView` | 전체화면 (내용이 많습니다) |
| 프로필 편집 | `ProfileEditSheet` | 시트 |
| 사진 크게 보기 | `ImageViewerModal` | 전체화면, 확대·이동 |
| 대화 검색 | `ChatSearchBar` | 대화방 상단 바 자리를 검색창이 덮음 |

화면 이동은 Navigation Compose 하나로 처리합니다. 대화방은 오른쪽에서 밀려 들어오고
뒤로가기로 나갑니다 — 안드로이드 표준이자 카카오톡 동작입니다.

---

## 5. 계층별 이식 계획

### 그대로 옮겨지는 것 (순수 계산, 가장 쉬움)

화면도 API도 모르는 코드라 Kotlin으로 번역만 하면 됩니다. 시험도 그대로 옮겨집니다.

| 맥 판 | 안드로이드 | 줄 수 |
|---|---|---|
| `ConversationCompactor` | 그대로 | 238 |
| `TokenEstimator` | 이미지 크기만 `BitmapFactory` 로 | 69 |
| `MathExpression` | 그대로 | 200 |
| `StreamingBubbleBuffer` | 그대로 | 116 |
| `AIModel` 의 요금표 | 그대로 | 219 중 일부 |

**여기부터 시작합니다.** 화면 없이 검증할 수 있어서 초반에 확신을 벌어 둘 수 있습니다.

### 갈아엎어야 하는 것

| 맥 판 | 안드로이드 | 왜 |
|---|---|---|
| `URLSession` + SSE | **OkHttp** + SSE 직접 파싱 | 맥 판도 `alt=sse`를 직접 파싱합니다. 형태가 같습니다 |
| `KeychainStore` | **EncryptedSharedPreferences** | 안드로이드의 키체인 대응물 |
| `StorageService` (JSON 파일) | 같은 방식, `filesDir` 로 | JSON 형식을 그대로 유지합니다 |
| `UserDefaults` | `SharedPreferences` / DataStore | |
| `MathGraphRenderer` (CoreGraphics) | **Compose Canvas** | 그리기 명령이 거의 1:1입니다 |
| `AttachmentImageCache` (NSImage) | Coil + 자체 캐시 | |
| `NSPasteboard` 붙여넣기 | 클립보드 + 키보드 이미지 입력 | |
| 파일 드래그·드롭 | **Photo Picker / 문서 선택** | 권한이 필요 없는 방식으로 |
| `WindowManager` | 없어짐 | |
| `BrightnessSlider` | 없어짐 | |

### 새로 필요한 것

- **ViewModel + StateFlow** — 화면 회전과 앱 재개에서 상태를 지키려면 필요합니다.
  맥 판은 `ObservableObject` 싱글턴으로 버텼는데 안드로이드에서는 그러면 안 됩니다.
- **Pretendard 폰트 번들** — 맥에는 시스템에 깔려 있지만 안드로이드에는 없습니다.
  OFL 라이선스라 앱에 넣어도 됩니다. Regular·Bold 두 벌이면 충분하고,
  한글 서브셋이 아니면 한 벌에 2MB쯤 됩니다.
- **적응형 앱 아이콘** — 앞판/뒷판 두 겹 + 구형 기기용 PNG.
  `AppIcon.iconset`의 그림을 재료로 씁니다.
- **뒤로가기 처리** — 검색 중이면 검색 닫기, 아니면 화면 나가기.

---

## 6. 어려울 것으로 보이는 곳

계획 단계에서 미리 짚어 둡니다. 여기서 시간이 샙니다.

### (1) 수식 렌더링 — 가장 큰 위험

맥 판은 말풍선마다 `WKWebView`를 띄우고 KaTeX로 수식을 그립니다.
자원(KaTeX 596KB + markdown-it 124KB)은 이미 앱 안에 들어 있어 오프라인에서도 됩니다.
그건 안드로이드 `assets/`로 그대로 옮기면 됩니다.

문제는 **안드로이드 WebView가 훨씬 무겁다**는 것입니다. 인스턴스 하나에 수십 MB를 쓰고
생성도 느립니다. 말풍선마다 하나씩 만들면 긴 대화에서 스크롤이 끊깁니다.

세 갈래를 두고, **실제로 재 보고** 정합니다.

1. **WebView 재활용 풀** — 맥 판 구조를 그대로. 화면에 보이는 개수만큼만 만들어 돌려씁니다.
   먼저 시도합니다.
2. **오프스크린 → 비트맵** — 수식을 한 번 그려 그림으로 만들고 그 뒤로는 이미지로 표시.
   스크롤은 가장 매끄럽지만 글자 선택·복사를 잃습니다.
3. **수식 없는 말풍선은 아예 네이티브로** — 마크다운만 있는 말풍선은 Compose
   `AnnotatedString`으로 그립니다. 실제로 대부분의 말풍선이 여기 해당할 것 같은데,
   **아직 세어 보지 않았습니다.** 1번과 함께 쓰면 효과가 클 것으로 봅니다.

여기가 "맥 판보다 부족하지 않게"의 성패가 갈리는 지점입니다.

### (2) 스트리밍 중 스크롤

말풍선이 자라는 동안 계속 맨 아래에 붙어 있어야 하는데, 수식 말풍선은 KaTeX가
다 그린 뒤에야 높이가 정해집니다. 맥 판은 높이가 확정될 때마다 알림을 쏘아
다시 내리는 방식으로 풀었습니다(`bubbleHeightSettled`). 같은 문제가 그대로 옵니다.

### (3) 키보드와 입력창

3-(5)에 적은 그대로입니다. 첨부 미리보기가 붙으면 입력창 높이가 변하고,
그 상태에서 키보드가 오르내리므로 계산이 겹칩니다.

### (4) 배경으로 밀려났다 돌아올 때

안드로이드는 앱을 언제든 메모리에서 내릴 수 있습니다. 스트리밍 중에 그러면
답변이 반만 남습니다. 최소한 **받던 것을 저장해 두고 돌아왔을 때 이어 보이게**
해야 합니다. 맥에는 없던 문제입니다.

### (5) API 키

APK는 뜯어보면 안이 다 보입니다. **키를 코드에 넣으면 안 됩니다.**
사용자가 설정 화면에서 직접 넣고 EncryptedSharedPreferences에 담습니다.
맥 판과 같은 방식입니다.

---

## 7. 작업 순서

각 단계 끝에서 실행되는 앱이 나오게 끊었습니다. 절반쯤에서 멈춰도 뭔가는 남습니다.

| 단계 | 내용 | 끝났을 때 |
|---|---|---|
| **0** | 도구 설치 (JDK 17, Android SDK, Gradle), 빈 프로젝트 빌드 | APK가 한 번 나옵니다 |
| **1** | 순수 계산부 이식 + 시험 | 화면 없이 검증 완료 |
| **2** | 테마·폰트·아이콘, 탭바 껍데기 | 눌러서 탭이 바뀝니다 |
| **3** | 저장 계층, 목록 두 개 (친구·채팅) | 저장된 방이 보입니다 |
| **4** | 대화방 화면, 말풍선, 입력창, 키보드 | 글자만 주고받습니다 |
| **5** | API 연결 + 스트리밍 | **실제로 대화가 됩니다** |
| **6** | 수식·마크다운 렌더링 | 6-(1)의 성능 판단을 여기서 |
| **7** | 첨부·사진 보기·그래프 | |
| **8** | 검색, 말투 편집기, 사용량 화면 | 기능 동등 달성 |
| **9** | 앱 아이콘, 반응형 점검(320/360/412dp), 릴리스 APK | 설치 가능한 결과물 |

3~5단계가 전체의 절반쯤 될 것으로 봅니다.

---

## 8. 정해야 할 것

시작 전에 답이 필요합니다.

1. **APK 서명** — 두 갈래입니다.
   - *디버그 서명* — 바로 옆에 두고 설치. 간단하고 별도 비밀번호가 없습니다.
   - *릴리스 서명* — 키 저장소(keystore)를 만들어야 하고 비밀번호가 붙습니다.
     나중에 스토어에 올리거나 업데이트를 배포하려면 이쪽이어야 합니다.

   **디버그 서명을 권합니다.** 혼자 쓰실 거면 그걸로 충분하고, 키 저장소는
   잃어버리면 같은 앱으로 업데이트를 못 하게 되는 물건이라 필요해질 때 만드는 게 낫습니다.

2. **패키지 이름** — 맥 판은 `com.sapiens.kakaotalk`을 씁니다(이름을 바꾼 뒤에도
   데이터가 날아가지 않게 그대로 뒀습니다). 안드로이드는 새로 만드는 것이니
   깨끗한 이름을 쓸 수 있습니다. 예: `com.sapiens.gagaodok`

3. **설치해서 시험할 기기** — 실기기가 있으면 USB로 바로 넣어 볼 수 있습니다.
   없으면 에뮬레이터를 씁니다(SDK에 딸려 옵니다만 몇 GB 더 듭니다).
   **반응형은 에뮬레이터로 폭을 바꿔 가며 보는 게 오히려 낫습니다.**

4. **맥 판의 대화 내역을 옮길지** — 저장 형식은 같은 JSON으로 맞출 예정이라
   파일을 복사하면 옮겨집니다. 필요하시면 그 통로를 만들어 두겠습니다.

---

## 9. 빌드 환경 준비 (0단계 상세)

이 맥의 현재 상태입니다. 확인해 봤습니다.

| 필요한 것 | 현재 |
|---|---|
| JDK 17+ | **없음** (Corretto 11만 있음) |
| Android SDK | **없음** |
| Gradle | **없음** (Gradle Wrapper가 알아서 받으므로 따로 안 깔아도 됩니다) |
| Android Studio | **없음** (명령줄만으로도 빌드는 됩니다) |
| Homebrew | 있음 |

받아야 할 용량은 대략 이렇습니다.

- JDK 17 — 약 200MB
- Android SDK (platform-tools, build-tools, platform 35) — 약 1.5GB
- 에뮬레이터를 쓰면 시스템 이미지 — 추가 1.5GB 내외

전부 홈 폴더(`~/Library/Android/sdk`)에 들어가고 **관리자 비밀번호는 필요 없습니다.**

한 가지 걸리는 게 있습니다. 설치 중에 **구글 Android SDK 라이선스에 동의**해야 하는데,
이건 회원님 이름으로 하는 약관 동의라 제가 대신 누르기 전에 허락을 받아야 합니다.
직접 누르고 싶으시면 그 명령만 따로 드리겠습니다.

---

## 10. 이 계획에서 아직 근거가 없는 것

나중에 "재서 정한 값"으로 오해하지 않도록 적어 둡니다.

- **말풍선 최대 폭 68%** — 짐작입니다. 안드로이드 카카오톡을 재서 확정해야 합니다.
- **글꼴 배율 0.85–1.3** — 흔히 쓰는 범위일 뿐, 카카오톡이 실제로 쓰는 값이 아닙니다.
- **수식 말풍선의 비율** — 6-(1)의 3번 방안이 얼마나 효과 있을지는 세어 봐야 압니다.
- **3~5단계가 절반** — 감입니다.
- **모바일 카카오톡의 아이콘·간격 치수** — 아직 하나도 재지 않았습니다.
  맥 판의 실측값은 데스크톱 것이라 여기 쓸 수 없습니다.
