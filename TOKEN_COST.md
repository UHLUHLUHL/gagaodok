# 턴이 쌓일수록 요금이 왜 그렇게 오르는가

2026-08-17. 실제 사용 중인 방 하나(마린, Gemini 3.7 Flash)의 사용량 화면 숫자에서
요금을 분해하고, 어디를 고쳐야 하는지 정리한 것입니다.

계기는 관찰이었습니다 — **"턴이 쌓일수록 토큰이 기하급수적으로 늘어난다. 캐시는
잘 터지는 것 같은데도 절대량이 감당이 안 될 것 같다."**

## 결론부터

1. 기하급수(지수)가 아니라 **제곱**입니다. 턴이 2배면 요금은 4배입니다.
2. 캐시는 실제로 잘 터지고 있습니다 — **토큰의 82%가 적중.** 관찰이 맞았습니다.
3. 그런데 그 82%가 **요금의 14%밖에 안 됩니다.** 돈은 캐시를 *만드는* 데(35%)와
   캐시를 *못 쓴* 요청(32%)에서 나갑니다. 적중률을 더 올려도 남는 게 별로 없습니다.
4. 지금 구조에는 **문맥 길이의 상한이 없습니다.** 압축은 증가 속도를 늦출 뿐입니다.
   이것 하나가 제곱의 원인입니다.

## 잰 것

사용량 화면에 뜬 숫자는 넷입니다.

| | 값 |
|---|---:|
| 누적 입력 | 2,572,393 토큰 |
| 누적 출력 | 38,598 토큰 |
| 캐시에 올린 토큰 | 507,733 토큰 |
| 캐시로 아낀 금액 | ₩1,391.38 ($0.9798) |
| 표시 요금 | ₩1,553.75 ($1.0942) |

먼저 **출력 1토큰당 입력 67토큰**이라는 비율 자체가 진단입니다. 매 턴 대화 전체를
다시 보내기 때문입니다.

### 캐시 적중 토큰 역산

화면에 안 나오는 값입니다. `TokenUsageStore.costUSD`의 식을 그대로 세우면 미지수는
캐시 적중 토큰 `C` 하나뿐이라 풀립니다. 단가는 `AIModel.kt`의 도입가
(입력 $0.75/M, 캐시입력 $0.075/M, 출력 $3.75/M, 보관 $0.50/M·시간)입니다.

```
보관량 = 507,733 × (900초/3600초) = 126,933 토큰·시간 → $0.0635

1.0942 = (2,572,393 − C)×0.75/M      ← 일반 입력
       + C×0.075/M                    ← 캐시 입력
       + 507,733×0.75/M               ← 캐시 생성 (입력 단가로 계상)
       + 38,598×3.75/M                ← 출력
       + 0.0635                       ← 보관

→ C = 2,110,000
```

교차 검증: `costWithoutCacheUSD` = 2.572393×0.75 + 0.038598×3.75 = **$2.0740**.
아낀 금액 = 2.0740 − 1.0942 = **$0.9798** → ₩1,391.4. 화면의 ₩1,391.38과 일치합니다.
역산이 맞습니다.

### 요금 분해

| 항목 | 토큰 | 금액 | 비중 |
|---|---:|---:|---:|
| **캐시 생성(업로드)** | 507,733 | $0.381 | **35%** |
| **일반 입력(캐시 미적중)** | 462,000 | $0.347 | **32%** |
| 출력 | 38,598 | $0.145 | 13% |
| 캐시 입력 | 2,110,000 | $0.158 | 14% |
| 캐시 보관 | — | $0.064 | 6% |
| | | **$1.094** | |

**요금의 3분의 2가 "캐시를 언제 만드느냐"에 달려 있습니다.** 적중률이 아니라.

## 왜 제곱인가

매 턴 이력 전체를 다시 보내므로 N턴째 요청은 N에 비례하고, 누적은 N²/2에 비례합니다.

압축은 이걸 **없애지 못하고 계수만 줄입니다.** `ConversationCompactor`는 원문 구간을
20~70턴 사이로 묶어 두지만, 구간 요약이 50턴마다 1,476토큰씩 **영구히 쌓입니다.**

```
문맥(N) ≈ 시스템지침 3,000 + (N/50)×1,476 + 45턴×턴당토큰
        = 3,000 + 29.5N + 4,725
```

턴당 증가가 105 → 29.5로 3.5배 줄지만 여전히 1차식이고, 누적은 여전히 제곱입니다.
2,000턴이면 문맥 65,814토큰. **상한이 없습니다.**

## 고칠 곳 (효과 순)

### ① 세션 첫 캐시를 미루지 말 것 — 즉시 −18~25%

`AIServicePrefixCache.kt`의 burst 게이트(`CACHE_BURST_WINDOW_MILLIS`, 5분)가
직전 요청이 오래됐으면 캐시를 안 만듭니다. 그래서 **매 세션 처음 두 턴이 문맥 전체를
전액으로** 나갑니다. 이게 "일반 입력 32%"의 큰 몫입니다.

당시 판단은 주석에 그대로 적혀 있습니다 — *"안 쓸 캐시를 만드는 것보다 낫습니다."*
문맥이 작을 때는 맞았지만 지금은 뒤집혔습니다.

| | 손해 |
|---|---|
| 안 쓸 캐시 하나 만들기 | 문맥 × (0.75 생성 + 0.125 보관)/M = **0.875 × 문맥** |
| 캐시 없이 한 번 보내기 | 문맥 × (0.75 − 0.075)/M = **0.675 × 문맥** |

한 세션이 2턴만 넘어가면 즉시 생성이 이깁니다. 묶음 길이에 대한 민감도
(1,000턴, 세션 간격 3시간):

| 한 묶음 턴수 | 2 | 3 | 4 | 6 | 9 | 15 | 30 |
|---|---:|---:|---:|---:|---:|---:|---:|
| 즉시 생성 효과 | −25% | −25% | −23% | −20% | −18% | −12% | −7% |

**어느 경우에도 손해가 없고, 짧게 자주 쓸수록 이득이 큽니다.** 관측된 캐시 업로드량이
모형보다 33% 많은 것으로 보아 실제 세션은 짧은 쪽이므로 −20% 이상 기대됩니다.

### ② 2차 압축 — 제곱을 끊는 유일한 방법

구간 요약 조각 수에 상한(예: 8개)을 두고, 넘으면 **원문에서 다시 굵게 요약**합니다.
문맥이 18,582토큰에 고정됩니다.

`ConversationSegment` 주석의 *"이전 요약을 다시 넣어 새 요약을 만들면 사본의 사본이
되어 초반 내용이 형체를 잃는다"* 는 우려는 **요약을 요약할 때** 이야기입니다.
**원문은 로컬에 그대로 남아 있으므로 원문에서 다시 뽑으면 그 열화가 없습니다.**
굵기만 달라지고 세대가 겹치지 않습니다. 400턴 재요약 비용은 1회 약 $0.03.

| | 500턴 | 1,000턴 | 2,000턴 | 2,000턴 문맥 |
|---|---:|---:|---:|---:|
| 현재 | $2.88 | $8.18 | $26.55 | 65,814 |
| ①만 | $2.39 | $6.74 | $21.46 | 65,814 |
| ①+② | $2.37 | **$5.19** | **$10.87** | **18,582** |

2,000턴에서 **59% 절감**이고, 무엇보다 문맥이 더 이상 자라지 않습니다.

### ③ 사진이 원문 구간에 영구히 남습니다 (과외 방 한정)

`buildGeminiContents`가 원문 구간의 모든 첨부를 매 턴 다시 싣습니다. 사진 한 장이
516토큰이므로 40턴이면 20,640토큰이 매 요청에 붙습니다.

요약 지침에는 *"사진을 주고받았다면 무엇을 찍은 사진이었는지"* 라는 항목이 이미
있는데, **원문 구간에는 같은 개념이 없습니다.** N턴 지난 사진은
`[사진: 무엇을 찍은 것]` 텍스트로 바꾸는 것이 맞습니다.

마린 방은 역산에서 사진 0으로 수렴해 해당 없지만, **과외 방에서는 이것이 최대 항목일
수 있습니다.** 아직 실측하지 않았습니다 — 과외 방 사용량으로 확인이 필요합니다.

## 하지 말아야 할 것

### TTL 연장 (지금은 안 됨)

캐시 생성이 35%라 TTL(`CACHE_TTL_SECONDS`, 900초)을 늘리고 싶어지지만,
**세션 간격이 1시간을 넘으면 오히려 손해입니다.** 보관료가 재생성비를 넘습니다.
(1,000턴, 묶음 9턴, ① 적용 상태 기준. 100%가 TTL 15분)

| 세션 간격 | TTL 30분 | 60분 | 120분 |
|---|---:|---:|---:|
| 15분 | 91% | **77%** | **72%** |
| 30분 | 104% | **82%** | **78%** |
| 1시간 | 104% | 113% | 90% |
| 2시간 | 104% | 113% | 130% |
| 3시간+ | 104% | 113% | 130% |

**손익분기가 실제 사용 패턴 바로 위에 걸쳐 있어서, 어느 쪽인지 모르면 도박입니다.**
그리고 앱은 세션 간격을 기록하지 않습니다. `lastRequestAt`에 직전 시각이 이미 있으니
방별 복귀 간격 분포를 몇 주 쌓아 TTL을 적응적으로 정하는 것이 순서입니다.
상수만 바꾸는 것은 근거가 없습니다.

## 이미 최적인 것 — 손대지 말 것

캐시 갱신 기준 `tail ≥ max(2000, tokenCount/5)`.

갱신 주기 r의 총비용은 `0.75 × (캐시크기×턴당증가/r + r/2)`이고, 미분하면 최적은
`r = √(2 × 캐시크기 × 턴당증가)`입니다. 캐시 5만 토큰·턴당 800토큰이면 **8,911**인데
현재 규칙이 내는 값이 **10,000**입니다. 거의 최적입니다.

(`tokenCount/5`가 정확히 최적이 되는 지점은 캐시크기 = 50×턴당증가, 즉 약 4만 토큰.
지금 문맥이 그 근처입니다. 우연히 잘 맞은 것이 아니라 맞는 자리에 있습니다.)

## 근거 재현

`tools/costsim.py`에 시뮬레이터가 있습니다. `AIServicePrefixCache.kt`와
`ConversationCompactor.kt`의 상수·규칙을 그대로 옮겼고, 압축이 접두사를 바꿔 캐시를
무효화하는 것과 보관료를 실제 보관 시간으로 매기는 것까지 반영했습니다.

```bash
python3 tools/costsim.py fit      # 관측 4개 값에 맞는 대화 조건 역산
python3 tools/costsim.py compare  # 개선안별 절감 효과
python3 tools/costsim.py sens     # 묶음 길이·TTL 민감도
```

가장 잘 맞는 조건은 **234턴 / 턴당 100토큰 / 한 묶음 13턴**이었습니다.

**모형의 한계를 적어 둡니다.** 총입력(오차 0.1%)과 총요금(오차 5.8%)은 잘 맞지만
**캐시 생성량은 19.5% 적게 나옵니다**(모형 408,840 / 관측 507,733). 실제 세션이
모형보다 짧거나, 압축 전 문맥이 더 컸다는 뜻입니다. 어느 쪽이든 ①의 효과는 **표의
값보다 커집니다** — 민감도 표에서 묶음이 짧을수록 이득이 크기 때문입니다. 그래서
①의 권고는 이 오차에 안전한 방향입니다. ②는 문맥 상한에만 의존하므로 영향받지
않습니다.

반대로 **③(압축 문턱 낮추기)은 이 오차에 안전하지 않습니다.** 장기 효과가 1~2%라
오차 범위 안에 들어갑니다. 그래서 권고에 넣지 않았습니다.

## 다음

- [ ] ① 첫 캐시 즉시 생성 — 상수·게이트 한 곳. 맥·안드로이드 양쪽.
- [ ] ② 2차 압축 — 설계 필요. 요약을 새로 만드는 일이라 `ConversationCompactor`의
      계획 로직과 시험을 함께 봐야 합니다.
- [ ] ③ 과외 방 사용량으로 사진 항목이 실제로 큰지 먼저 확인.
- [ ] TTL은 세션 간격 기록을 붙인 뒤에.

## 같이 볼 문서

- `MAC_BACKPORT.md` — 요금 관련 15개 항목을 고친 기록
- `ARCHITECTURE.md` — 어느 파일을 열어야 하는지
